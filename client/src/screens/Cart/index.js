import React from 'react';
import {View} from 'react-native';

const Cart = () => {
  return <View></View>;
};

Cart.navigationOptions = () => {
  return {
    title: 'Cart',
  };
};

export default Cart;
